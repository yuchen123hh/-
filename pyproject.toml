[project]
name = "audio-event-poc"
version = "0.1.0"
description = "Quiet-office CLAP audio event detection PoC for knock, cough, and clap."
requires-python = ">=3.10,<3.13"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
